const Footer = () => (
  <footer className="w-full py-4 bg-cyan-700 text-center text-amber-50 text-sm mt-auto">
    <span>
      Made with <span className="text-red-400">♥</span> &copy; {new Date().getFullYear()} BlinkURL
    </span>
  </footer>
);

export default Footer;
